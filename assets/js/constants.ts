export enum DOM {
  CANVAS_ID = "canvas",
  SOUND_BUTTON_ID = "sound",
  MENU_ITEMS = "#menu .item",
}

export enum CH {
  TREE_NEW = "tree:new",
}

export enum SOUND {
  NOISE_PROBABILITY = 0.89,
  BG_VOLUME = 0.5,
}