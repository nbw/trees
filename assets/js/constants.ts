export enum DOM {
  CANVAS_ID = "canvas",
  SOUND_BUTTON_ID = "sound",
  MENU_ITEMS = "#menu .item",
}

export enum BOUND {
  X = 10000,
  Y = 10000,
}

export enum CH {
  TREE_NEW = "tree:new",
  TREE_DESTROY = "tree:destroy",
}

export enum SOUND {
  NOISE_PROBABILITY = 0.93,
  BG_VOLUME = 0.5,
}

export enum COLOR {
  LEAF_GREEN = "#389859",
}